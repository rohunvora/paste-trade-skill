{
  "id": "trade-slash-wrapper",
  "name": "Trade Slash Wrapper",
  "description": "Guarantees an immediate /trade acknowledgment, queues same-chat runs in a private worker lane, and keeps chat updates concise.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
